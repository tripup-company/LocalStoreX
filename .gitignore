# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
#dist

# Compiled output emitted next to the TypeScript sources. `tsc` is a
# typecheck-only step now (see tsconfig "noEmit"), so nothing here is
# expected — this keeps a stray build from being committed again.
src/**/*.js
src/**/*.js.map
src/**/*.d.ts
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
